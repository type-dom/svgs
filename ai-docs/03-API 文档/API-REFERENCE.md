# API Reference (TypeDoc Generated)

> **注意**: 本文档由 TypeDoc 自动生成，请勿手动编辑。
>
> **Note**: This document is auto-generated by TypeDoc. Do not edit manually.

## 📖 概述 / Overview

本文档包含 `@type-dom/svgs` 的完整 API 参考，通过 TypeDoc 从源代码中的 TypeScript 注释和类型定义自动生成。

This documentation contains the complete API reference for `@type-dom/svgs`, automatically generated by TypeDoc from TypeScript comments and type definitions in the source code.

---

## 🔗 访问方式 / Access Methods

### 1. 本地预览 / Local Preview

```bash
# 生成文档 / Generate documentation
npm run docs:api

# 打开文档 / Open documentation
open GENERATED/api-extractor/index.html
```

### 2. 监听模式 / Watch Mode

```bash
# 开发时自动更新 / Auto-update during development
npm run docs:watch
```

### 3. 发布前生成 / Pre-publish Generation

```bash
# 发布到 NPM 前自动生成 / Auto-generate before publishing to NPM
npm run prepublishOnly
```

---

## 📁 文档结构 / Document Structure

生成的文档将包含以下内容：

Generated documentation will include:

```
GENERATED/api-extractor/
├── index.html              # API 首页 / API Homepage
├── modules.html            # 模块列表 / Module List
│
├── modules/                # 模块文档 / Module Documentation
│   ├── common-index.html   # Common 类别导出
│   ├── element-plus-index.html  # Element Plus 类别导出
│   └── fluentui-index.html      # FluentUI 类别导出
│
├── classes/                # 类文档 / Class Documentation
│   ├── TdAddSvg.html
│   ├── TdCloseSvg.html
│   ├── ... (所有 SVG 组件 / All SVG components)
│   └── SvgComponent.html   # 基础组件类 / Base component class
│
├── interfaces/             # 接口文档 / Interface Documentation
│   ├── SvgComponentProps.html    # 组件 Props 接口
│   ├── ComponentConfig.html      # 组件配置接口
│   └── ... (其他接口 / Other interfaces)
│
├── types/                  # 类型别名 / Type Aliases
│   ├── SvgSize.html        # 尺寸类型定义
│   ├── ColorValue.html     # 颜色值类型
│   └── ... (其他类型 / Other types)
│
└── typedoc.json            # JSON 格式文档 / JSON format documentation
```

---

## 📝 包含内容 / Included Content

### 导出入口 / Export Entry Points

TypeDoc 将从以下入口文件生成文档：

TypeDoc will generate documentation from these entry points:

1. **主入口 / Main Entry**
   - `src/index.ts` - 完整库导出
   - 包含所有分类的重新导出

2. **Common 类别 / Common Category**
   - `src/lib/common-index.ts`
   - 通用图标集合 (add, close, check 等)

3. **Element Plus 类别 / Element Plus Category**
   - `src/lib/element-plus-index.ts`
   - Element Plus 风格图标

4. **FluentUI 类别 / FluentUI Category**
   - `src/lib/fluentui-index.ts`
   - FluentUI 风格图标

### 文档元素 / Documentation Elements

每个导出的符号将包含：

Each exported symbol will include:

- ✅ **类型签名 / Type Signatures**
- ✅ **JSDoc 注释 / JSDoc Comments**
- ✅ **参数说明 / Parameter Descriptions**
- ✅ **返回值类型 / Return Type**
- ✅ **使用示例 / Usage Examples**
- ✅ **相关符号链接 / Related Symbol Links**
- ✅ **源码位置 / Source Location**
- ✅ **版本信息 / Version Info**

---

## 🔧 TypeDoc 配置 / TypeDoc Configuration

详细配置请查看 [`typedoc.json`](../../typedoc.json):

For detailed configuration, see [`typedoc.json`](../../typedoc.json):

```json
{
  "entryPoints": [
    "./src/index.ts",
    "./src/lib/common-index.ts",
    "./src/lib/element-plus-index.ts",
    "./src/lib/fluentui-index.ts"
  ],
  "out": "./GENERATED/api-extractor",
  "includeVersion": true,
  "excludePrivate": true,
  "excludeProtected": false,
  "excludeExternals": true,
  "validation": {
    "notExported": true,
    "invalidLink": true,
    "notDocumented": false
  }
}
```

### 配置说明 / Configuration Notes

| 选项               | 说明         | 当前设置                  |
| ------------------ | ------------ | ------------------------- |
| `entryPoints`      | 文档生成入口 | 4 个索引文件              |
| `out`              | 输出目录     | `GENERATED/api-extractor` |
| `includeVersion`   | 包含版本号   | ✅ true                   |
| `excludePrivate`   | 排除私有成员 | ✅ true                   |
| `excludeProtected` | 排除保护成员 | ❌ false (包含)           |
| `excludeExternals` | 排除外部依赖 | ✅ true                   |

---

## 📊 质量保障 / Quality Assurance

### 验证规则 / Validation Rules

TypeDoc 会自动检查：

TypeDoc will automatically check:

- ✅ **未导出符号 / Not Exported**: 识别未导出的内部符号
- ⚠️ **无效链接 / Invalid Links**: 检测损坏的内部链接
- ℹ️ **未文档化符号 / Not Documented**: 报告缺少注释的符号 (警告)

### 同步更新 / Synchronization

API 文档与源码保持同步：

API documentation stays synchronized with source code:

```
源码修改 → TypeDoc 重新生成 → API 文档更新
Source change → TypeDoc regeneration → API doc update
```

**最佳实践 / Best Practices**:

1. 每次修改源码后运行 `npm run docs:watch`
2. 发布前必须运行 `npm run docs:api`
3. 审查生成的文档确保完整性

---

## 🎯 使用场景 / Use Cases

### 场景 1: 查找组件 API

```markdown
需求：想了解 TdAddSvg 的所有可用 props

步骤:

1. 打开 GENERATED/api-extractor/classes/TdAddSvg.html
2. 查看 "Properties" 部分
3. 每个 prop 都有类型和默认值说明
```

### 场景 2: 了解类型定义

```markdown
需求：想知道 SvgComponentProps 的具体结构

步骤:

1. 导航到 interfaces/SvgComponentProps.html
2. 查看所有必需和可选字段
3. 点击相关类型跳转到详细定义
```

### 场景 3: 探索库结构

```markdown
需求：快速了解整个库的组织结构

步骤:

1. 从 index.html 开始
2. 浏览 modules 了解分类
3. 查看每个模块的导出符号
4. 使用搜索功能快速定位
```

---

## 🔗 相关链接 / Related Links

### 内部文档 / Internal Documentation

- [项目概述](../01-项目概述/项目背景.md) - 项目背景和目标
- [技术栈](../01-项目概述/技术栈.md) - 使用的技术栈说明
- [编码规范](../02-开发规范/编码规范.md) - TypeScript 编码规范

### 外部资源 / External Resources

- [TypeDoc 官方文档](https://typedoc.org/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [JSDoc 规范](https://jsdoc.app/)

---

## 📝 维护指南 / Maintenance Guide

### 日常维护 / Routine Maintenance

```bash
# 开发时：监听模式自动更新
npm run docs:watch

# 发布前：生成最终版本
npm run docs:api

# 验证：检查文档完整性
open GENERATED/api-extractor/index.html
```

### 故障排查 / Troubleshooting

**问题 1**: 文档未更新

```bash
# 解决方案：清理并重新生成
rm -rf GENERATED/api-extractor
npm run docs:api
```

**问题 2**: 链接损坏

```bash
# TypeDoc 会报告无效链接
# 根据错误信息修复源码中的引用
npm run docs:api 2>&1 | grep "invalid link"
```

**问题 3**: 缺少符号

```bash
# 检查是否已正确导出
# 查看 typedoc.json 的 exclude 配置
# 确认 JSDoc 注释完整
```

---

## 📈 统计信息 / Statistics

### 预期规模 / Expected Scale

基于当前项目结构：

Based on current project structure:

| 类别         | 预计数量 |
| ------------ | -------- |
| **SVG 组件** | ~500 个  |
| **接口定义** | ~10 个   |
| **类型别名** | ~5 个    |
| **工具函数** | ~20 个   |
| **总页面数** | ~600 页  |

### 生成性能 / Generation Performance

```
典型生成时间:
- 首次完整生成：~15 秒
- 增量更新 (watch 模式): <2 秒
- 文件大小：~50MB (HTML + assets)
```

---

## 📝 更新日志 / Changelog

| 日期       | 版本   | 更新内容                   |
| ---------- | ------ | -------------------------- |
| 2026-03-19 | v1.0.0 | 初始版本，TypeDoc 配置完成 |

---

**最后更新 / Last Updated**: 2026-03-19  
**维护者 / Maintainer**: TypeDom Team  
**生成工具 / Generator**: TypeDoc ^0.27.0
