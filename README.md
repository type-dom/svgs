# svgs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build svgs` to build the library.

## Running unit tests

Run `nx test svgs` to execute the unit tests via [Vitest](https://vitest.dev/).
